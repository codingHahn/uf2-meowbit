#include <stdint.h>
#define CHARACTER_WIDTH 5

#ifndef ALPHABET_H
#define ALPHABET_H
extern uint8_t A[CHARACTER_WIDTH];

extern uint8_t B[CHARACTER_WIDTH];

extern uint8_t C[CHARACTER_WIDTH];

extern uint8_t D[CHARACTER_WIDTH];

extern uint8_t E[CHARACTER_WIDTH];

extern uint8_t F[CHARACTER_WIDTH];

extern uint8_t G[CHARACTER_WIDTH];

extern uint8_t H[CHARACTER_WIDTH];

extern uint8_t I[CHARACTER_WIDTH];

extern uint8_t J[CHARACTER_WIDTH];

extern uint8_t K[CHARACTER_WIDTH];

extern uint8_t L[CHARACTER_WIDTH];

extern uint8_t M[CHARACTER_WIDTH];

extern uint8_t N[CHARACTER_WIDTH];

extern uint8_t O[CHARACTER_WIDTH];

extern uint8_t P[CHARACTER_WIDTH];

extern uint8_t Q[CHARACTER_WIDTH];

extern uint8_t R[CHARACTER_WIDTH];

extern uint8_t S[CHARACTER_WIDTH];

extern uint8_t T[CHARACTER_WIDTH];

extern uint8_t U[CHARACTER_WIDTH];

extern uint8_t V[CHARACTER_WIDTH];

extern uint8_t W[CHARACTER_WIDTH];

extern uint8_t X[CHARACTER_WIDTH];

extern uint8_t Y[CHARACTER_WIDTH];

extern uint8_t Z[CHARACTER_WIDTH];
#endif /*ALPHABET_H*/
